# 프로젝트 진행 기록 (Notes)

## 최근 변경
- 2026-04-28: 코드 리뷰 정리(/simplify) — 3개 리뷰 에이전트(재사용·품질·효율)의 발견 사항을 일괄 적용. (1) **`update_monitor_config` 인자 sprawl 제거**: 8개 평탄 파라미터(`cpu, gpu, memory, network, battery, show_charging_icon, charging_icon_size, charging_icon_distance`)를 단일 `MonitorConfigPayload` 구조체(`#[serde(rename_all = "camelCase")]`)로 통합. 새 필드 추가 시 시그니처/호출 양쪽 동시 수정 부담 제거. emit 페이로드도 `&config` serialize로 단순화. 프론트엔드 `MainWindow.tsx`/`SettingsWindow.tsx` 3곳의 invoke 호출을 `{ config: updated }` 형태로 변경. (2) **CPU/메모리/GPU emit dedup**: Thread 1에 `prev_cpu_pct`/`prev_mem_pct`/`prev_gpu_pct` (i32::MIN sentinel) 추가, 정수 단위 변화 시에만 emit. idle 시스템에서 동일 값 반복 emit으로 인한 React `setState` → `evaluateMessages` 재실행 ~분당 60회 제거. (3) **Thread 1 thread-local 상태 단일 소스화**: 10개 reset 가능 변수(`battery_elapsed_ms`/`cached_battery_percent`/`prev_charging`/`monitor_refresh_elapsed_ms`/`network_refresh_elapsed_ms`/`prev_monitor_count`/`prev_on_fullscreen`/`prev_cpu_pct`/`prev_mem_pct`/`prev_gpu_pct`)를 `PollingThreadState` 구조체와 `fresh()` 생성자로 통합. 첫 진입 = `let mut state = PollingThreadState::fresh();`, 중지→시작 reset = `state = PollingThreadState::fresh();` 한 줄. 변수 추가 시 init/reset drift 위험 제거. Thread 2는 외부 의존(rng/monitors_guard/last_update) 변수가 많고 reset 블록이 한 위치에 모여 있어 변경 가치 < 비용으로 판단해 보존. (4) **`DEFAULT_MONITOR_CONFIG` 상수**: `types.ts`에 추출, `MainWindow.tsx`/`SettingsWindow.tsx`에서 인라인 리터럴 중복 제거. `gpu` 필드 추가 시 발현된 drift 위험 즉시 차단. (5) **`compute_file_sha256` allocation 절감**: `digest.iter().map(|b| format!("{:02x}", b)).collect()`(per-byte String 32회 할당) → `String::with_capacity(64)` + `write!` 32회. 첫 String 1회만 할당. (6) **`extract_sha256_from_body` 단순화**: 32줄 수동 byte 인덱스 루프 → `body.split(|c: char| !c.is_ascii_hexdigit()).find(|tok| tok.len() == 64).map(|s| s.to_ascii_lowercase())` 3줄. 동일 동작, 가독성 향상. (7) **`SettingsWindow.tsx` 모듈 스코프 hoist**: `UpdateStatus`/`UpdateInfoDto` 타입 정의를 컴포넌트 함수 내부 → 모듈 스코프로 이동(매 렌더 재선언 회피). `MSG_TARGET_BADGE_KEY: Record<string, string>` 룩업 테이블 신설 → `msg.target` → 배지 i18n 키 ternary 체인 5단을 `t(MSG_TARGET_BADGE_KEY[msg.target] ?? 'msg.badgeNetUp')` 한 줄로 교체. **사용자 체감 변경 없음** (모두 내부 구조 개선이며 emit dedup도 동일 값 emit 차단이라 표시값 동일). `cargo check`(3.12s, 경고 없음) / `tsc --noEmit` 통과.
- 2026-04-28: NSIS 설치 마법사 다국어 표시 — `src-tauri/tauri.conf.json`의 `bundle.windows.nsis.languages` 배열을 `["English"]` → `["English", "Korean", "Japanese", "SimpChinese", "TradChinese"]`로 확장하여 앱이 지원하는 5개 언어(en/ko/ja/zh/zh-Hant)와 정렬. Tauri NSIS의 기본 다국어 동작(`displayLanguageSelector`=false)은 시스템 UI 언어와 일치하는 NLF가 있으면 자동 사용하고, 없으면 배열의 첫 항목(English)을 fallback으로 사용. 따라서 한국어/일본어/중국어(간체·번체) Windows에서는 그 언어로 자동 표시되고, 그 외 시스템에서는 영어로 표시됨. `nsis-hooks.nsi`의 `GetUserDefaultUILanguage` 1042 분기(한글 표시 이름 "테스크몬" 적용)는 그대로 유지 — 요청 범위 외이며 한국 사용자 표시 이름 한글화 동작은 보존. 코드 동작 변경 없음(인스톨러 빌드 산출물에만 영향). JSON 구문 유효성 `node -e JSON.parse(...)` 통과. 실제 NSIS 빌드 검증은 `tauri build` 실행 시점에서 수행됨(이번 작업에선 빌드 미실행).
- 2026-04-28: 트레이 "중지 → 시작"을 앱 재실행과 동일한 초기 상태로 reset — 사용자 보고: 오류로 캐릭터가 이상 동작 시 트레이 토글만으로는 복구되지 않고 앱 재실행 시에만 정상화. 앱 재실행과 동일한 수준의 reset이 필요하다는 요구. (1) **`src-tauri/src/lib.rs` start 핸들러 강화**: 기존 `is_running.store(true)` + 창 show + always_on_top + 트레이 메뉴 재빌드만 수행하던 동작에 (a) atomic 공유 상태 reset (`shared_teleport_x → i64::MIN`, `shared_needs_redraw → false`, `shared_on_fullscreen → false`)과 (b) **`webview.eval("window.location.reload()")` 호출**을 추가. webview reload로 React 모든 useState/useRef가 초기 상태로 되돌아가지만 LocalStorage 사용자 설정(펫·언어·알림·메시지 등)은 유지됨 — 가장 흔한 React state corruption 원인을 처리. setup 클로저 외부에 `teleport_tray`/`needs_redraw_tray`/`on_fullscreen_tray` Arc clone 3개 추가하여 트레이 핸들러가 capture 가능하도록 함. (2) **Thread 1 prev_running 토글 감지 + thread-local 캐시 reset**: 매 루프 시작에서 `is_running` load → false면 `prev_running = false; sleep; continue`, true이면서 `!prev_running`일 때 `battery_elapsed_ms = 178_000`(2초 후 첫 측정), `cached_battery_percent = -1`(배터리 잔량 무효화), `prev_charging = false`, `monitor_refresh_elapsed_ms = 10_000`(즉시 enumerate), `network_refresh_elapsed_ms = 30_000`(즉시 NIC 재구성), `prev_monitor_count = 0`, `prev_on_fullscreen = false`로 reset 후 GPU PDH baseline throwaway poll 1회. (3) **Thread 2 prev_running_t2 토글 감지 + thread-local 변수 reset**: `move_phase`/`prev_move_phase`/`current_y`/`climb_edge_x`/`climb_top_y`/`climb_bottom_y`/`consecutive_climbs`/`smooth_y`/`smooth_y_init`/`prev_target_x`/`prev_target_y`/`prev_cursor_on_pet`/`prev_mouse_enabled`/`cross_pending`/`cross_exit_y`/`cross_neighbor`/`sorted_indices`/`dir_resync_after_change`/`dir_sync_tick`/`zorder_tick`/`prev_on_fs_t2`/`prev_scale`/`random_dir_left`/`prev_random_dir_left`/`last_update`를 모두 첫 진입 시점과 동일한 초기값으로 되돌리고, `current_x`는 `all_monitors_reader.read()`로 가장 왼쪽 모니터의 `x - 100` 위치로 reset(앱 시작 시 line 1330~1343의 init 로직과 동일). 24개 변수 reset. (4) **`Releases.md` UI/UX 개선** 섹션에 사용자 체감 항목 추가. 사용자가 트레이 시작을 누르면 (a) atomic 공유 상태 정리 → (b) is_running true 전환 → (c) Thread 1/2가 다음 루프에서 자체적으로 thread-local 변수 reset → (d) webview reload로 React state 초기화 → 결과적으로 앱 종료 후 재실행한 것과 거의 동일한 상태로 복구. 사용자 LocalStorage 설정은 유지되므로 펫·언어·알림 등 설정 손실 없음. `cargo build`(9.31s, 경고 없음) / `tsc --noEmit` / `vite build`(487.02 kB) 통과.
- 2026-04-28: 모니터링 폴링 최적화 (A안 비활성 항목 폴링 회피 + B안 시간 기반 tick) — (1) **`src-tauri/src/lib.rs` `MONITOR_FLAG_*` 비트마스크 신설**: `MONITOR_FLAG_GPU = 1<<0`, `MONITOR_FLAG_NETWORK = 1<<1`, `MONITOR_FLAG_BATTERY = 1<<2`, `MONITOR_FLAGS_DEFAULT = 0b111` 모두 활성. CPU/메모리는 펫 이동 속도 + 메시지 평가에 항상 필요하므로 flag 대상에서 제외. (2) **`AppState.monitor_flags: Arc<AtomicU8>`** 필드 추가, `setup()`에서 `shared_monitor_flags = Arc::new(AtomicU8::new(MONITOR_FLAGS_DEFAULT))` 생성하여 manage 등록 + 폴링 스레드(t1)에 clone 전달. (3) **`update_monitor_config` 커맨드 시그니처에 `state: State<'_, AppState>` 추가**: gpu/network/battery 매개변수에서 비트 합성 후 `state.monitor_flags.store(flags, Relaxed)`로 갱신. (4) **폴링 스레드 분기**: 매 루프 시작 시 `monitor_flags_reader.load(Relaxed)`로 1회 read → `gpu_enabled`/`network_enabled`/`battery_enabled` 지역 변수. GPU PDH `poll()`은 `gpu_enabled`일 때만, `networks.refresh()`+iter+emit은 `network_enabled`일 때만, `battery_manager.batteries()`+충전 상태 emit은 `battery_enabled`일 때만 실행. CPU/메모리는 그대로 매 폴링 측정·emit. (5) **B안: tick 시간 기반 전환**: `monitor_refresh_tick: u32 % 10` → `monitor_refresh_elapsed_ms: u64 >= 10_000`, `network_refresh_tick: u32 % 30` → `network_refresh_elapsed_ms: u64 >= 30_000`. 매 폴링 끝에 `saturating_add(interval)`로 누적, 임계값 초과 시 작업 + 0으로 리셋. 폴링 간격이 5초·30초여도 모니터 enumerate는 10초, NIC 인터페이스 재구성은 30초 절대 간격 보장. 시작값을 임계값과 동일하게 두어 첫 폴링에서 즉시 1회 실행(기존 동작 유지). 네트워크 비활성 상태에서도 누적은 유지하여 재활성 시 즉시 인터페이스 갱신. (6) **`src/MainWindow.tsx` init sync**: 마운트 useEffect 시작 시점에 `invoke('update_monitor_config', { ...monitorConfig }).catch(() => {})` 1회 호출. spread 결과 객체 리터럴로 `InvokeArgs` 타입 호환 확보 (MonitorConfig interface는 인덱스 시그니처가 없어 직접 전달 시 TS 오류). 앱 시작 직후 사용자 끄기 설정 즉시 백엔드에 반영. (7) **체감 효과**: GPU off 사용자: PDH 호출 0회/s, 네트워크 off: NIC 통계 조회 0회/s, 배터리 off: starship_battery 호출 0회 + 충전 상태 GetSystemPowerStatus 호출 0회. 폴링 간격을 길게 잡은 사용자도 모니터 hot-plug 감지가 10초 내로 보장. 사용자 노출 동작 변화 없음(내부 최적화). `cargo build`(8.17s, 경고 없음) / `tsc --noEmit` / `vite build`(487.02 kB) 통과.
- 2026-04-28: GPU 모니터링 토글 누락 2건 수정 — 직전 GPU 추가 작업에서 누락된 두 곳을 보강. (1) **`src-tauri/src/lib.rs` `update_monitor_config` 커맨드 시그니처에 `gpu: bool` 매개변수 추가**: SettingsWindow에서 `invoke("update_monitor_config", updated)` 호출 시 Tauri는 frontend 객체 키를 함수 매개변수에 1:1로 매핑하므로, 매개변수에 `gpu`가 없으면 토글 결과가 백엔드에 도달하지 못하고 `monitor-config-update` 이벤트의 페이로드에서도 누락되어 메인 윈도우의 `monitorConfig.gpu`가 항상 false로 머무는 문제. emit 페이로드 JSON에도 `"gpu": gpu` 추가. (2) **`src/MainWindow.tsx:980` 말풍선 표시 조건**: `(monitorConfig.cpu || monitorConfig.memory || monitorConfig.network || monitorConfig.battery)`에 `monitorConfig.gpu` OR 항이 빠져 있어, GPU만 단독 체크된 상태에서는 말풍선 컨테이너 자체가 렌더되지 않던 문제. `cargo check`(1.82s) / `tsc --noEmit` / `vite build`(486.97 kB) 통과.
- 2026-04-28: GPU 사용률 모니터링 추가 (Windows PDH GPU Engine 카운터) — (1) **`src-tauri/Cargo.toml`**: `[target.'cfg(windows)'.dependencies]` 섹션 신설, `windows = { version = "0.58", features = ["Win32_System_Performance", "Win32_Foundation"] }` 추가. (2) **`src-tauri/src/lib.rs` `gpu_monitor` 모듈 신설**: Windows 작업 관리자(taskmgr.exe)와 동일한 PDH 카운터 `\GPU Engine(*)\Utilization Percentage` 사용. `GpuMonitor::new()`에서 `PdhOpenQueryW`+`PdhAddEnglishCounterW`(영어 로케일 — 시스템 언어 무관)로 모든 어댑터·엔진 인스턴스 매칭, 첫 호출은 baseline으로 무시. `poll()`은 `PdhCollectQueryData`+`PdhGetFormattedCounterArrayW` 2단계 호출(1차 PDH_MORE_DATA로 버퍼 크기 조회 → 2차 실제 값 수집)로 모든 인스턴스 사용률을 합산 후 `min(100.0)`으로 캡 — 작업 관리자 표시 방식. `Drop`에서 `PdhCloseQuery`. 비-Windows용 더미 stub(`new() = None`, `poll() = None`)으로 cross-platform 컴파일 보장. (3) **폴링 스레드(라인 ~1232) 통합**: 배터리 매니저 초기화 다음에 `gpu_mon = GpuMonitor::new()` 시도(실패해도 패닉 없음), CPU/메모리 emit 다음(라인 ~1339)에 `if let Some(ref mon) = gpu_mon { if let Some(gpu_pct) = mon.poll() { emit("gpu-usage", gpu_pct.round() as u32) } }`. (4) **`src/types.ts`**: `MonitorConfig`에 `gpu: boolean` 필드, `PetMessage.target` 주석에 `"gpu"` 추가. `evaluateMessages` 시그니처에 `gpu: number` 인자 추가, `case "gpu"` 분기 신설. (5) **`src/locales.ts`** 5개 사전(ko/en/ja/zh/zhHant) 모두에 `monitor.gpu`/`msg.targetGpu`/`msg.badgeGpu` 3개 키씩 추가 — 191 → 194개. (6) **`src/MainWindow.tsx`**: `gpuUsage` state, `monitorConfig` 기본값 `gpu: false`, `gpu-usage` 이벤트 리스너(+ cleanup), 말풍선 stat-row의 CPU 다음에 `monitorConfig.gpu && <span>🎮 GPU {gpuUsage}%</span>` 추가, `evaluateMessages` 호출과 useEffect deps에 `gpuUsage` 반영. (7) **`src/SettingsWindow.tsx`**: `monitorConfig` 기본값 `gpu: false`, `gpuUsage = 0` 미리보기 상수, 모니터링 항목 토글에 GPU 라벨(CPU 다음, 메모리 앞), 메시지 `target` select에 `<option value="gpu">{t('msg.targetGpu')}</option>`, 등록된 메시지 배지 매핑에 `gpu` 분기 추가. (8) **`README.md`** 주요 기능에 GPU 추가 + 주요 의존성 목록에 `windows` crate 추가. (9) **`Releases.md`** v0.1.0의 `### 새로운 기능`에 GPU 모니터링 항목 추가. 작업 관리자 표시 방식(엔진별 사용률 합산 후 100% 캡)으로 NVIDIA/AMD/Intel 등 제조사 무관 동작. 첫 폴링 직후 ~1초간 0%로 표시되는 PDH 동작 특성 있음. `cargo build`(21.61s, windows crate 포함, 경고 없음) / `tsc --noEmit` / `vite build`(486.97 kB) 통과. 5개 사전 키 일치 검증 완료(194개 동일).
- 2026-04-28: 다국어 확장(일본어/중국어 간체·번체) + 업데이트 확인 실패 표시 변경 — (1) **`src/locales.ts` 다국어 5종 확장**: `Language` 타입을 `'system' | 'ko' | 'en' | 'ja' | 'zh' | 'zh-Hant'`로 확장. `ja`(日本語), `zh`(简体中文), `zhHant`(繁體中文) 사전 신규 작성(각 191개 키 전수 번역). 모든 사전(ko/en/ja/zh/zhHant)에 `general.langJa`/`general.langZh`/`general.langZhHant` 키 추가. `translations` 객체를 `Record<ResolvedLanguage, ...>`로 타입 강화하고 키에 `'zh-Hant'` 사용. `detectSystemLanguage()`/`resolveLanguage()` 반환 타입을 `ResolvedLanguage = 'ko' | 'en' | 'ja' | 'zh' | 'zh-Hant'`로 확장. 시스템 언어 감지 분기: `ko*` → `ko`, `ja*` → `ja`, `zh-tw`/`zh-hk`/`zh-mo`/`zh-hant*` → `zh-Hant`, 그 외 `zh*` → `zh`, 나머지 → `en`. (2) **`src/SettingsWindow.tsx`**: 언어 select에 `<option value="ja">`/`<option value="zh">`/`<option value="zh-Hant">` 3개 옵션 추가. (3) **업데이트 확인 실패 표시 변경 (`src/SettingsWindow.tsx:387`)**: `check_update` `.catch()` 핸들러에서 `setUpdateStatus('error')` → `setUpdateStatus('latest')`로 변경. 네트워크 오류·GitHub API 차단 등으로 확인이 실패해도 "업데이트 확인에 실패했습니다." 대신 "최신 버전입니다."로 표시. 다운로드 실패(line 414)의 `'error'`는 그대로 유지(상이한 컨텍스트). (4) **`README.md`**: 주요 기능과 일반 설정 섹션의 언어 목록을 `한국어 / English / 日本語 / 简体中文 / 繁體中文`로 갱신. (5) **`Releases.md`**: v0.1.0 항목에 `### 새로운 기능`(다국어 확장)과 `### UI/UX 개선`(업데이트 확인 실패 시 최신 버전 표시) 서브섹션 추가. 사전 키 누락 검사: 5개 사전 모두 191개로 동일 키 집합 확인. JS 번들 461.13 → 486.09 kB(번역 데이터 추가, 약 25 kB 증가). `tsc --noEmit` / `vite build` / `cargo check` 통과.
- 2026-04-28: v0.1.0 배포 직전 정비 2차 + 프로젝트 지침 보강 — (1) **`CLAUDE.md`(프로젝트 지침) 섹션 추가**: "## Releases.md 작성 방법" 신설. 갱신 규칙(현재 버전 확인 → 동일 버전이 있으면 합치고 날짜를 오늘로, 없으면 상단에 새 버전 추가), 작성 대상(사용자 체감 변경만, 내부 리팩토링·메타데이터·주석 수정은 제외), 표현 가이드(한국어, 짧고 명확, 코드/구현 설명 금지), 5개 서브섹션(`### 새로운 기능` / `### UI/UX 개선` / `### 보안` / `### 정리` / `### 버그 수정`), 기본 템플릿 포함. (2) **`Releases.md` 신규 작성**: v0.1.0 (2026-04-28) 항목으로 SHA256 무결성 검증·체크섬 부재 시 사용자 확인 다이얼로그(보안), 인스톨러 메타데이터 보강·SmartScreen 안내 추가(정리)를 기재. (3) **잔여 파일 정리**: `git rm src-tauri/check_output.txt`(이전 프로젝트명 `my-pet` 시절 로그), `git mv messages.json samples/messages.json`(샘플 데이터, 코드 import 없음). (4) **자동 업데이트 SHA256 무결성 검증 추가**: `sha2 = "0.10"` 의존성 추가, `lib.rs`에 `extract_sha256_from_body`(릴리즈 노트 body에서 영숫자 경계 기준 64자 hex 토큰 첫 매칭 추출, 정규식 의존성 회피)와 `compute_file_sha256`(64KB 청크 스트리밍) 헬퍼 신설, `UpdateInfo.sha256: Option<String>` 필드 추가, `check_update`에서 `body` 파싱 후 채워 프론트엔드로 전달, `download_and_install_update`는 `expected_sha256: Option<String>` 매개변수를 받아 일치 시에만 spawn — 불일치 시 받은 파일을 `std::fs::remove_file`로 자동 삭제하고 예상/실제 해시를 포함한 에러 반환. 체크섬이 없는 릴리즈는 프론트엔드에서 `ask` 다이얼로그로 진행 여부 확인 후 `expectedSha256: null` 전달(검증 생략). (5) **`locales.ts` 한·영 키 추가**: `about.updateNoChecksumPrompt`. (6) **`README.md`** 정보 섹션 설명을 새 동작에 맞춰 갱신(검증 흐름·체크섬 부재 시 확인 안내 명시). `cargo check`(13.47s, sha2 컴파일 포함, 경고 없음) / `tsc --noEmit` / `vite build`(461.12 kB) 통과.
- 2026-04-28: v0.1.0 배포 직전 정비 1차 — (1) **`Cargo.toml` 메타데이터 보강**: 기본 템플릿값(`authors = ["you"]`, `description = "A Tauri App"`)을 실제 정보로 교체하고 `license = "MIT"`, `repository`, `homepage` 필드 추가. crates.io 게시 의도는 없지만 인스톨러 메타데이터·LICENSE 일관성·GitHub 메타데이터 표시용. (2) **`tauri.conf.json` bundle 메타데이터 추가**: `publisher`, `copyright`, `category = "Utility"`, `shortDescription`, `longDescription` 추가. NSIS 설치 마법사 / "프로그램 추가/제거" 항목 / 시작 메뉴 툴팁에 노출되는 정보. (3) **`.gitignore` 정리**: `src-tauri/target/`, `src-tauri/check_output.txt` 항목 추가(이전 프로젝트명 `my-pet` 시절의 컴파일 에러 로그가 추적되고 있던 문제 1차 차단; 실제 파일 삭제는 별도 승인 후 진행). (4) **README 배포 안내 보강**: `## 설치` 섹션에 (a) SHA256 체크섬으로 무결성 검증 가능 안내 + `Get-FileHash` 예시, (b) 무서명 인스톨러로 인한 SmartScreen 경고 대응 안내(`추가 정보 → 실행`) 두 항목 추가. NSIS 파일명도 실제 산출물 형식(`TaskMon-Setup-vX.Y.Z.exe`)에 맞춰 정정. `cargo check`(3.20s) / `tsc --noEmit` 통과. 코드 동작 변경 없음.
- 2026-04-28: 콘솔창 깜빡임 제거 — `lib.rs`의 외부 자식 프로세스(`reg`/`curl`) 호출 5곳에 Windows `CREATE_NO_WINDOW`(0x0800_0000) `creation_flags` 적용. 원인: GUI 부모 프로세스(WebView2 앱)가 콘솔 서브시스템 프로그램(`reg.exe`/`curl.exe`)을 spawn할 때 Windows가 자동으로 conhost 콘솔을 할당하여, `Command::output()` 실행 동안 콘솔 창이 짧게 생성-소멸되며 화면에 깜빡임 발생. 특히 설정 창 마운트 시 `SettingsWindow.tsx:247`의 `useEffect`가 `get_auto_start`를 즉시 invoke하여 `reg query`가 실행되는 경로에서 사용자 가시 증상이 가장 두드러짐. 수정 내역: (1) 모듈 상단에 `#[cfg(target_os = "windows")] const CREATE_NO_WINDOW: u32 = 0x0800_0000;` 추가, (2) 각 호출 블록에 `use std::os::windows::process::CommandExt;` 추가, (3) `Command::new(...).args(...).creation_flags(CREATE_NO_WINDOW).output()` 체인으로 5곳 일괄 변경 — `get_auto_start` 1곳, `set_auto_start` 2곳(add/delete), `check_update` 1곳(curl), `download_and_install_update` 1곳(curl). 인스톨러 실행(`Command::new(&installer_path).spawn()`)은 NSIS GUI 프로세스라 콘솔 미할당이므로 변경 대상에서 제외. 비-Windows 빌드는 `#[cfg(target_os = "windows")]` 가드로 영향 없음. `cargo check` 통과(경고 없음).
- 2026-04-28: 제거(uninstall) 시 잔여 파일/폴더 자동 정리 — `src-tauri/nsis-hooks.nsi`에 `NSIS_HOOK_POSTUNINSTALL` 매크로 신규 추가, `RMDir /r "$LOCALAPPDATA\TaskMon"`로 설치 폴더 전체를 재귀 삭제. 기존 NSIS 표준 제거 동작은 설치 매니페스트에 등록된 파일만 지우므로, WebView2 런타임이 생성한 `EBWebView/`(LocalStorage·캐시·쿠키 등) 폴더가 잔존하는 문제가 있었음. 설치 경로는 `NSIS_HOOK_PREINSTALL`에서 `$LOCALAPPDATA\TaskMon`로 강제 고정되어 있으므로 사용자 임의 경로 위험 없음. 부수효과: 사용자가 제거하면 LocalStorage에 저장된 펫·알림·메시지 설정도 함께 삭제됨(재설치 시 초기 상태로 시작). NSIS 스크립트라 별도 빌드 검증 불가 — 표준 NSIS 명령 + 기존 매크로 동일 패턴으로 작성.
- 2026-04-28: 좌측 모드 등반/하강 phase 회전 방향 버그 수정 — `MainWindow.tsx:927`의 `cumulativeRotateDeg` 계산식을 `-90 * step` → `(isLeftMode ? 90 : -90) * step`으로 변경. 원인: CSS 개별 속성 `rotate`/`scale`은 합성 시 scale → rotate 순으로 적용되는데(translate→rotate→scale 선언 순서이지만 좌측에서 우측으로 행렬 곱하면 가장 우측=scale이 점에 먼저 적용), 좌측 모드(`scale: -1 1`)에서 같은 부호(-90°)로 회전하면 펫 local (0, +60)이 (60, 0)으로 가서 컨테이너의 **오른쪽**에 위치 → 좌측 벽이 아닌 화면 안쪽으로 멀어지고, 말풍선(0, -25)도 (-25, 0)으로 가서 벽 너머(화면 밖)에 위치. 좌측 모드에서 회전 부호를 반대(+90°)로 적용하면 미러와 결합되어 펫이 좌측 벽에 닿고 말풍선이 화면 안쪽으로 향함. (Phase 1=좌측 등반, Phase 3=우측 하강 모두 동일 원리로 해결). 부수효과: 좌측 모드 Phase 1/3에서 말풍선 텍스트 읽기 방향이 우측 모드와 거울대칭(우측 모드와 다른 방향이지만 모드 내부적으로는 일관됨). 모드 전환은 사용자 명시적 액션이거나 random 모드의 화면 밖 지점이라 회전 점프 가시성 없음. `tsc --noEmit` / `cargo check` 통과.
- 2026-04-28: 코드 리뷰 정리(/simplify, 프로젝트 완료 단계) — (1) **`SettingsWindow.tsx` 정보 탭 업데이트 상태 블록 중복 제거**: 5개 `{updateStatus === '...' && <div className="about-row">...}` 블록(checking/available/latest/downloading/error)을 단일 conditional 블록 1개로 통합. `'available'`(클릭 가능한 버튼)만 별도 분기 처리하고, 나머지 4개 안내 텍스트 케이스는 `updateStatus` → 클래스명/번역 키 매핑(`about.update${PascalCase}`)으로 단일 렌더 경로 사용. ~25줄 → ~10줄로 축소. (2) **`MainWindow.tsx:911` 주석 드리프트 정정**: `isLeftMode` 판정 주석에 `mode 5=기본 이동(반복)` 누락된 항목 추가(코드는 `mode === 4 || mode === 5` 모두 `randomDirLeft` 참조). 기능 변화 없음. JS 번들 461.09 → 460.61 kB. `tsc --noEmit` / `vite build` / `cargo check` 통과.
- 2026-04-28: 시스템 부하 시 펫 이동/sprite 애니메이션 동기화 + 누적 회전 단순화 — (1) **속도 수식 일치 (핵심 수정)**: React `MainWindow.tsx`의 `cpu-usage` 리스너 수식을 `1 + cpu/10`(max 11×) → `1 + cpu/25`(max 5×)로 변경하여 Rust `lib.rs:1394`의 `speed_multiplier`와 동일하게 맞춤. 이전엔 펫 위치 이동(Rust)과 sprite 프레임 애니메이션(React playbackRate)이 다른 비율로 가속되어 시스템 부하 시 "이동은 하는데 프레임은 따로 도는" 현상이 발생. 이제 양쪽이 동일 비율로 가속되어 동기화됨. (2) **`phaseStep` state 제거**: useState + useEffect로 누적 step을 관리하던 패턴을 ref만 사용하는 render-time 갱신으로 단순화. movePhase 변경 시 추가 렌더 1회 절약, 동일 phase 재진입 시 if 검사로 StrictMode 이중 호출에서도 사이클 중복 증가 없음. 누적 회전각 계산은 `phaseCycleRef.current * 4 + movePhase`로 동일 렌더에서 직접 파생. JS 번들 461.13 → 461.09 kB. `tsc --noEmit` / `cargo check` / `vite build` 통과.
- 2026-04-28: 등반/하강 phase 회전 보간 + 좌우 반전 분리 — (1) **MainWindow.tsx**: `.pet-container`의 `transform: scaleX(-1) rotate(...)` 단일 속성을 개별 CSS 속성 `rotate`/`scale`로 분리. 누적 step 카운터(`phaseStep = phaseCycleRef.current * 4 + movePhase`)를 도입해 phase 전환 시 항상 -90° 단방향 회전이 되도록 함(특히 Phase 3→0이 270° 장방향 회전으로 보이지 않음). (2) **App.css**: `.pet-container`에 `transition: rotate 0.2s ease-out` 추가 → 등반·하강 회전이 부드럽게 보간. `scale`(좌우 반전)에는 transition을 적용하지 않아 반전은 즉시 반영(보간 시 펫이 가늘어지는 부작용 회피). (3) `phaseCycleRef`는 정상 사이클(Phase 3→0) 종료 시에만 +1 증가 → 모드 전환 등으로 비정상 phase 변경이 와도 -90° 단방향 보간 유지. `tsc --noEmit` / `cargo check` / `vite build` 통과.
- 2026-04-28: 펫 좌우 반전 타이밍 및 화면 가장자리 동작 자연화 — (1) **bounce 모드(반복)**: 좌/우 끝 반전 임계값을 펫 스프라이트의 가시 경계 기준으로 대칭화. 기존엔 좌측에서는 윈도우가 한 개 너비만큼 화면 밖으로 빠져야 반전(저속 CPU에서 5초 이상 화면 밖 드리프트), 우측에서는 절반 보일 때 반전이라 비대칭이었음. 이제 `pet_left <= min_x` 또는 `pet_right >= max_x` 시 즉시 반전 + 펫 위치를 가시 경계에 클램프 → 저속 CPU에서도 펫이 화면 안에서 곧바로 좌우 반전이 보임. (2) **mode 0(기본 오른쪽) wrap**: 하드코딩된 `200.0`/`100.0`을 `actual_win_w` 기반 DPI 스케일 값으로 교체, 좌측 핫플러그 복구 조건을 mode 2(`< min_x - actual_win_w`)와 대칭화. (3) **방향 변경 이벤트 재발행 보강**: `move-direction` IPC 이벤트가 React 측에서 누락되지 않도록 `dir_resync_after_change` 카운터 추가, 방향 전환 후 6프레임(~100ms) 재발행. `cargo check` / `tsc --noEmit` 통과.
- 2026-04-28: 업데이트 인스톨러 저장 위치 변경 — `%TEMP%` → 사용자 `다운로드` 폴더(Tauri `app.path().download_dir()` 기반, Known Folder API 사용으로 사용자 변경된 경로도 정확히 반영). README의 정보 섹션 설명 갱신. `cargo check` 통과.
- 2026-04-28: GitHub 릴리즈 기반 자동 업데이트 확인/설치 기능 추가 — 정보 탭 진입 시 `check_update` async 커맨드로 GitHub Releases API(`/repos/jongcheol-pak/TaskMon/releases/latest`)를 호출하여 최신 태그를 조회하고, 현재 `CARGO_PKG_VERSION`보다 높으면 앱 버전 항목 아래에 "업데이트가 있습니다. v{버전}" 클릭 항목 표시. 클릭 시 `download_and_install_update` async 커맨드가 `TaskMon-Setup-v{버전}.exe`를 `%TEMP%`로 다운로드한 후 spawn으로 분리 실행하고 800ms 후 앱 자동 종료. 외부 의존성 추가 없이 `curl.exe`(Windows 10 1803+ 기본 포함)로 HTTP 처리, 응답 파싱은 기존 `serde_json` 사용. 두 커맨드 모두 `async fn`으로 선언하여 Tauri tokio 워커 풀에서 실행 → UI 스레드 프리징 없음. `locales.ts`에 `about.update*` 한·영 키 추가, `App.css`에 `.about-update-text`/`.about-update-error` 스타일 추가, README의 `정보` 섹션에 동작 설명 추가. `cargo check`/`tsc --noEmit` 통과.
- 2026-04-27: README.md 정리 + 프로젝트 지침 신규 — (1) `알려진 제한 사항` 섹션 제거(내용이 시스템 요구 사항/설정 저장 위치/라이선스 섹션에 모두 중복), (2) 펫 개수 표기 오류 정정(`23종` → `16종`, 코드 PET_TYPES와 일치), (3) 루트에 `CLAUDE.md` 신규 작성 — README 섹션 순서·스타일·금지 사항·갱신 기준 명시(현재 형식을 그대로 유지하도록 강제, MailTrayNotifier 형식을 기준으로 함).
- 2026-04-27: NSIS 설치 파일명을 `TaskMon_{버전}_x64-setup.exe`(Tauri 기본) → `TaskMon-Setup-v{버전}.exe`로 변경. (1) `scripts/rename-installer.cjs` 신규 — `tauri.conf.json`에서 버전 읽고 NSIS 출력 폴더의 `TaskMon_*-setup.exe` 패턴을 와일드카드 매칭하여 새 이름으로 rename(복수 후보 시 최신 mtime 선택, 동일 이름 결과물은 덮어쓰기), (2) `build.bat` 단계 [3/3] → [4/4]로 확장하여 빌드 직후 `node scripts/rename-installer.cjs` 자동 실행. 기존 NSIS 산출물로 스크립트 실행 검증 완료.
- 2026-04-27: WebView2 사용자 데이터 디렉터리를 `%LocalAppData%\com.jongc.taskmon`(bundle identifier 기본값) → `%LocalAppData%\TaskMon`(앱 설치 폴더와 동일)로 변경. (1) `tauri.conf.json`에서 메인 윈도우 정의 제거(`"windows": []`), (2) `lib.rs`에 `webview_data_directory()` 헬퍼 추가(LOCALAPPDATA + "TaskMon"), (3) `setup()` 클로저에서 메인 윈도우를 `WebviewWindowBuilder`로 코드 생성하면서 `.data_directory()` 적용, (4) `open_or_focus_settings()`의 설정 윈도우 빌더에도 동일 `.data_directory()` 적용. 메인 윈도우 옵션 6개(transparent/decorations/shadow/resizable/alwaysOnTop/skipTaskbar/visible/title/inner_size) 모두 빌더로 이전. 마이그레이션 코드 없음 — 기존 사용자는 첫 실행 시 모든 설정이 초기화됨(기존 `com.jongc.taskmon` 폴더는 자동 삭제 안 됨, 수동 정리 필요). README의 설정 저장 위치 표 갱신. `cargo check` 통과.
- 2026-04-27: README에 정보 탭 + 펫 말풍선 알림 캡처 추가 — 알림 등록 단계에 `bubble-notification.png`(펫 머리 위 말풍선 표시 예시) 인라인 추가, 일반 설정 다음에 `정보` 섹션 신규(`settings-about.png` 표시 + 정보 탭 설명).
- 2026-04-27: README.md를 MailTrayNotifier 형식으로 전면 개편 — 중앙 정렬 로고/타이틀/설명, 주요 기능 불릿, 시스템 요구 사항, 설치(Releases/소스 빌드), 사용 방법(스크린샷 8장 포함), 폰트/일반 설정 별도 섹션, 설정 저장 위치 표, 주요 의존성 링크, 알려진 제한 사항, 라이선스/Credits/저작권 섹션. `docs/screenshots/` 디렉터리에 캡처 10장 배치(`pet-running`, `tray-menu`, `settings-pet/movement/monitoring/messages/alarm/timer/font/general`). 파일명 오타 2건 정정(`settings-about.png` → `settings-font.png`, `bubble-notification.png` → `settings-general.png`, 실제 캡처 내용에 맞춤).
- 2026-04-27: 몬스터(1)/몬스터(2)/자동차(1) 펫 삭제 + Finn 이름 변경 + 정보 탭 제작자 표기 — `PET_TYPES`에서 `monster1`/`monster2`/`car1` 항목 제거, `finn`의 `name`을 'Finn' → '사람(2)'로 변경(id는 기존 사용자 설정 호환을 위해 'finn' 유지). locales.ts 한/영 키 정리(`pet.monster1`/`pet.monster2`/`pet.car1` 삭제, `pet.finn` 값 '사람(2)'/'Human (2)'로 변경). 이미지 파일 10개(`Monster1_*.png` 4개, `Monster2_*.png` 4개, `Car1_*.png` 2개) 삭제. 정보 탭 itch.io 링크 아래에 제작자 X 태그(@ArksDigital, @LazyHamsters) 표기 추가(`about.assetsCredits` 키). App.css에 `.about-credits` 스타일 추가.
- 2026-04-27: 설정 화면에 "정보" 탭 추가 — 앱 버전, 프로젝트 라이선스(MIT 기반 + 출처 표시 의무), GitHub 저장소 링크, 이미지 에셋 출처(itch.io) 표시. 외부 링크는 `@tauri-apps/plugin-opener`의 `openUrl`로 기본 브라우저 실행. 루트에 `LICENSE` 파일 신규 작성(코드 재사용 시 출처 표시 필수, 이미지 에셋은 별도 라이선스 명시). README에 라이선스/이미지 에셋 섹션 추가. locales.ts에 `sidebar.about`/`about.*` 한·영 번역 키 추가, App.css에 `.about-block`/`.about-row`/`.about-link` 등 정보 탭 전용 스타일 추가.
- 2026-04-27: 사람(2)/사람(4)/UFO(1) 펫 삭제 — `PET_TYPES`에서 `human2`/`human4`/`ufo1` 항목 제거. types.ts import 정리, locales.ts 한/영 번역 키(`pet.human2`/`pet.human4`/`pet.ufo1`) 삭제. 관련 이미지 파일 6개(`Human2_Hurt.png`/`Human2_Idle.png`/`Human2_Run.png`/`Human4_Idle.png`/`Human4_Run.png`/`UFO1_Run.png`) 삭제.

## 미해결 및 향후 과제
- 없음 (현재 모든 주요 기능 구현 완료)

## 주의사항
- `lib.rs` 수정 시 `cargo build`를 통해 Win32 API 호출부의 안정성을 항상 확인해야 함.
- 애니메이션 프레임 추가 시 `App.css`의 `background-size`와 `steps()` 값을 이미지 너비에 맞춰 갱신해야 함.
